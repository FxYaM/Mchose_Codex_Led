{
  "name": "mchose-codex-led",
  "version": "1.5.0",
  "private": true,
  "description": "MCHOSE K99 V2 lighting controller and Codex status bridge",
  "main": "ledctl.cjs",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check ledctl.cjs && node --check hook-handler.cjs && node --check codex-exec-led.cjs && node --check lib/codex-state-watcher.cjs"
  },
  "license": "UNLICENSED"
}
